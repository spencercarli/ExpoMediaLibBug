{
  "name": "expomedialibbug",
  "version": "1.0.0",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "build:android-dev": "npx eas-cli build --profile development --platform android",
    "build:ios-dev": "npx eas-cli build --profile development --platform ios",
    "build:ios-dev-simulator": "npx eas-cli build --profile development-simulator --platform ios",
    "build:all-preview": "npx eas-cli build --profile preview --platform all"
  },
  "dependencies": {
    "expo": "~48.0.6",
    "expo-media-library": "~15.2.2",
    "expo-status-bar": "~1.4.4",
    "react": "18.2.0",
    "react-native": "0.71.3"
  },
  "devDependencies": {
    "@babel/core": "^7.20.0"
  },
  "private": true
}
